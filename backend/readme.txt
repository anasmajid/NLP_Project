entry point: run.py
or you can input the command in my command.txt if you are using linux
you can follow the code examples to use
requirements are all in requirement.txt file
model takes in a list of strings
model.score will return a list. In case of a single input, just use returned_result[0]